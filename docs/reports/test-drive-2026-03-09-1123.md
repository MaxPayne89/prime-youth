# Test Drive Report - 2026-03-09 11:23

## Scope
- Mode: branch (`feat/338-add-provider-profiles-to-admin-dashboard` vs `main`)
- Files changed: 9
- Commits: 8
- Routes affected: `/admin/providers` (index, show, edit)

## Backend Checks

### Passed
- **Tests**: 2958 tests, 0 failures, 12 skipped
- **New test files**: 11/11 pass (3 admin_changeset tests, 8 provider_live tests)
- **admin_changeset valid input**: casts `verified: true` and `subscription_tier: "professional"` correctly
- **admin_changeset invalid tier**: rejects `"invalid_tier"` with `{:error, "is invalid"}`
- **admin_changeset provider-owned fields**: `business_name`, `phone`, `website`, `address`, `description` all ignored (empty changes map)
- **SubscriptionTiers derivation**: `provider_tiers/0` returns `[:starter, :professional, :business_plus]`, `@valid_tier_strings` matches `["starter", "professional", "business_plus"]`
- **DB persistence**: edit via UI correctly persisted `verified: true` and `subscription_tier: "professional"` to database

### Issues Found
- None

## UI Checks

### Pages Tested

#### `/admin/providers` (index) - PASS
- Page title "Providers" displayed
- Sidebar has "Providers" link with storefront icon
- Table columns: Business Name, Verified, Tier, Created At, Actions
- No "New" button (Delete button disabled, as expected)
- Show and Edit action buttons present per row
- All 3 tier labels render correctly: Starter, Professional, Business Plus
- Search box present
- Pagination working (8 total items)
- Sorting links on all orderable columns

#### `/admin/providers/:id/show` - PASS (with issue)
- All fields displayed: Business Name, Verified, Description, Phone, Website, Address, Categories, Created At
- Verified shows green checkmark when true
- Edit button present
- Readonly fields correctly shown as read-only

#### `/admin/providers/:id/edit` - PASS
- Business Name: disabled/readonly textbox (correctly protected)
- Verified: editable checkbox
- Tier: editable select with correct options (Starter, Professional, Business Plus)
- Provider-owned fields (description, phone, website, address) correctly excluded from edit form
- Save/Cancel buttons work correctly
- Success flash: "Provider has been edited successfully."

#### Mobile responsive (375x667) - PASS
- Backpex table collapses to Business Name + action icons
- Sidebar collapses automatically
- Header scales properly
- All rows visible with scroll

### Issues Found
- **[warning] Categories renders as concatenated string**: On the show page, the `categories` field (type `{:array, :string}`) displays as `"artmusicsports"` instead of `"art, music, sports"`. This is because `Backpex.Fields.Text` calls `to_string/1` on the array value, which concatenates list elements. Consider using a custom `render/1` override or a different field module that handles arrays.
  - Location: `lib/klass_hero_web/live/admin/provider_live.ex:100` (categories field definition)
  - Expected: "art, music, sports" (or comma-separated tags/badges)
  - Actual: "artmusicsports"

## Auto-Fixes Applied
- None

## Recommendations
1. **Fix categories display** (warning): Override the `categories` field with a custom `render/1` callback that joins the array with ", " or renders as tag badges. Alternatively, use `Backpex.Fields.InlineCRUD` or a custom field module.
2. **Consider adding a Verified column filter**: Backpex supports column filters that would let admins quickly filter by verified/unverified providers.
