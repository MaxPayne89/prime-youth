# Test Drive Report - 2026-04-01

## Scope
- Mode: branch
- Branch: `feat/361-assigned-staff-members-can-message-parents` vs `main`
- Files changed: 44
- Routes affected: none (no new routes; modifies existing messaging flows)

## Environment
- Elixir 1.20.0-rc.3, Phoenix, PostgreSQL (dev)
- Tidewave MCP for backend verification
- Chrome DevTools MCP for UI verification (Playwright browser context died mid-session)

## Backend Checks (Tidewave MCP)

### Passed

| Check | Description | Result |
|-------|-------------|--------|
| B1 | `Provider.assign_staff_to_program/1` creates assignment | PASS |
| B2 | `program_staff_participants` projection syncs via integration event | PASS |
| B3 | Staff auto-added to existing broadcast conversation as participant | PASS |
| B4 | `Messaging.send_message/3` — staff can send in broadcast (no :broadcast_reply_not_allowed) | PASS |
| B5 | `Messaging.create_direct_conversation/4` with program_id — staff auto-added as participant | PASS |
| B5b | Existing conversation without program_id returns without adding staff (design: new only) | PASS |
| B6 | `Provider.unassign_staff_from_program/2` — projection deactivated, staff remains in existing conversations | PASS |
| B7 | Zero error logs during all operations | PASS |

### Issues Found

- **info**: Projection sync is async (PubSub). Deactivation took ~500ms to reflect. Not a bug — expected behavior for event-driven projections.

## UI Checks (Chrome DevTools MCP)

### Pages Tested

| Check | Description | Result |
|-------|-------------|--------|
| U1 | Login as staff (uitest-staff@example.com), navigate to messaging | FAIL |
| U3 | Login as provider (claudia.wolf@example.com), /provider/messages | PASS |
| U3b | Provider broadcast thread — staff message attribution | PASS |

### Issues Found

- **warning**: Staff cannot access any messaging route
  - Steps to reproduce: Log in as uitest-staff@example.com → navigate to `/messages` or `/provider/messages`
  - Expected: Staff sees their conversations
  - Actual: Redirected to `/staff/dashboard`. `/messages` has `redirect_provider_or_staff_from_parent_routes` hook that kicks staff. `/provider/messages` requires `:provider` role which staff don't have.
  - Impact: Staff are added as conversation participants (backend works) but have no UI to view or send messages. The messaging link in the staff header (`/messages`) doesn't work.
  - Recommendation: Add messaging routes to the `:require_staff_provider` live session, or create a shared messaging session accessible to both provider and staff roles. This is a routing/authorization gap, not a bug in the staff messaging feature itself.

- **info**: Broadcast conversation list shows "Unknown" as `other_participant_name`
  - Steps to reproduce: Provider views `/provider/messages`, broadcast conversation row
  - Expected: A meaningful label (e.g., program name or "Broadcast")
  - Actual: Shows "Unknown" as the other participant name
  - Note: Pre-existing issue, not introduced by this branch. Broadcast summaries don't resolve a single "other" participant name since there are many participants.

## Edge Cases

| Check | Description | Result |
|-------|-------------|--------|
| B5b | Creating conversation without program_id does not add staff | PASS |
| B6 | Soft unassign — staff stays in conversations after unassignment | PASS |
| B2 | Re-assignment after unassign — projection reactivates | PASS |

## Auto-Fixes Applied
None

## Issues Filed
None (pending user decision on routing fix)

## Recommendations

1. **[Warning — Must address]** Add staff messaging routes to the router. Options:
   - Add `/staff/messages` routes under `:require_staff_provider` live session
   - Or broaden the `:authenticated` session's `/messages` route to not redirect staff
   - This should be a follow-up issue since it requires a routing decision

2. **[Info — Low priority]** The "Unknown" broadcast summary name is pre-existing. Consider showing the broadcast subject or program name instead.

## Screenshots

- `docs/reports/test-drive-2026-04-01-361-attribution.png` — Provider view of broadcast thread showing "Wolf Musik Akademie via UI TestStaff" attribution
