# Test Drive Report - 2026-03-15 (Broadcast Reply Privacy)

## Scope
- Mode: branch (main...HEAD)
- Files changed: 15 (source) + 3 (docs)
- Routes affected: `/messages/:id`, `/provider/messages/:id`

## Backend Checks (Tidewave MCP)

### Passed
- `get_user_id_for_provider/1`: resolves provider profile ID to user ID correctly; returns `:not_found` for invalid IDs
- `SendMessage` guard: parent rejected with `:broadcast_reply_not_allowed` in broadcast conversation
- `SendMessage` guard: provider allowed to send follow-up in own broadcast
- `ReplyPrivatelyToBroadcast` (after fix): creates direct conversation with both parent and provider as participants
- System note inserted with correct `[broadcast:ID]` token and subject
- Idempotency: second call reuses same conversation, no duplicate system notes

### Bug Found and Fixed
- **Critical**: `find_direct_conversation(provider_id, provider_user_id)` matched the wrong conversation when parent initiates. The provider is always a participant in their own conversations, so searching by `provider_user_id` returns the first matching conversation (belonging to a different parent). Fixed by splitting find (uses parent user_id) and create (adds both users via repo ports) in `ReplyPrivatelyToBroadcast`.

## UI Checks (Playwright MCP)

### Pages Tested
- `/messages/:id` (broadcast, parent view): PASS
  - Broadcast badge visible
  - No message input form
  - "Broadcast messages are one-way" text shown
  - "Reply privately" button present
  - Screenshot: `docs/reports/screenshots/broadcast-reply-bar-parent.png`

- "Reply privately" click: PASS
  - Navigates to `/messages/:direct_conversation_id`
  - Direct conversation shows message input (not reply bar)
  - System note visible: `[broadcast:ID] Re: Upcoming tournament info`
  - Title: "Conversation" (not "Broadcast")

### Not Tested (no provider login flow in this session)
- Provider view of broadcast conversation (covered by automated tests)
- Mobile viewport check

## Auto-Fixes Applied
- None needed

## Commits from Test Drive
- `b3d5a9c` fix: correct `find_direct_conversation` lookup in `ReplyPrivatelyToBroadcast`
